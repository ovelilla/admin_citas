const Error = ({children}) => {
    return (
        <div className="bg-red-700 text-white text-center mb-3 p-3 rounded-md uppercase font-bold">
            {children}
        </div>
    )
}

export default Error
